SELECT TOP 1 * FROM vm_WORK_ORDER WHERE (PART_ID = 'TF3555') ORDER BY BASE_ID DESC


SELECT COUNT ( BASE_ID) AS N, PART_ID FROM vm_WORK_ORDER GROUP BY PART_ID  ORDER BY COUNT ( BASE_ID) DESC


SELECT COUNT ( BASE_ID) AS N, PART_ID FROM vm_WORK_ORDER WHERE LEFT(PART_ID,2) = 'TF' GROUP BY PART_ID  ORDER BY COUNT ( BASE_ID) DESC